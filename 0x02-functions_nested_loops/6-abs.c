#include "main.h"

/**
 * _abs - Computes the absolute value of every int
 *
 * @e: int whose abs value to be determined
 *
 * Return: the abs value of the int
 */

int _abs(int e)
{
	if (e >= 0)
	{
		return (e);
	}

	else
	{
		return (-e);
	}
}
