#include "main.h"

/**
 * _strlen -  function will return the length of a string
 * @s: the string involved
 * Return: Always 0.
 */

int _strlen(char *s)
{
	int str;

	for (str = 0; s[str] != '\0'; str++)
	{
	}
	return (str);
}
